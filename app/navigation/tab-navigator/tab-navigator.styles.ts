import { stylesheet } from '~/utils/styles';

export const ICON_SIZE = 25;

export const TabNavigatorStyles = stylesheet({
  view: {
    bar: {
      backgroundColor: 'white',
    },
    hiddenBar: {
      height: 0,
    },
  },
});
